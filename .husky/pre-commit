#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# ESLint (report only — existing codebase has pre-existing warnings)
echo "  Linting..."
pnpm run lint 2>/dev/null || echo "Lint completed with warnings"

echo "Pre-commit checks passed."

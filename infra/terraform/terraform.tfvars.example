# Copy to terraform.tfvars (gitignored) and fill. Secrets go via env, not here:
#   export TF_VAR_nextauth_secret="$(openssl rand -base64 32)"
snowflake_role = "ACCOUNTADMIN"
database       = "CP_DATA360"
credit_quota   = 5

# Pointed at the LOCAL backend (uvicorn :8000) for BI-dashboard work — the local
# source carries the FQDN fix the remote lacks. Revert both lines to
# http://api.datalab360.io to go back to the deployed backend.
NEXT_PUBLIC_API_URL=http://api.datalab360.io
API_PROXY_UPSTREAM=http://api.datalab360.io
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=local-dev-secret-32chars-minimum-pad
NEXT_PUBLIC_APP_NAME=Data360
